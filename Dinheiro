#include <iostream>
using namespace std;

int main() {
    double valorHora, horasTrabalhadas;

    cout << "Digite o valor por hora do trabalhador: ";
    cin >> valorHora;

    cout << "Digite o numero de horas trabalhadas: ";
    cin >> horasTrabalhadas;

    double salarioTotal = valorHora * horasTrabalhadas;
    double descontoIRS = salarioTotal * 0.125;
    double salarioLiquido = salarioTotal - descontoIRS;

    cout << "Salario bruto: " << salarioTotal << " euros" << endl;
    cout << "Desconto IRS (12.5%): " << descontoIRS << " euros" << endl;
    cout << "Salário liquido: " << salarioLiquido << " euros" << endl;

    return 0;
}
